namespace Character.Combat.Skill.Modules
{
    public class SkillModule : Module
    {
        // + EntityType Flag;
        // + int InstanceID { get; set; }
        // + DataIndex ActionCode { get; protected set; }
        // + ICombatProvider Provider { get; protected set; }
    }
}
