using UnityEngine;

public class CoreSampleScript : MonoBehaviour
{
    public int LinkIntegerValue;
    
    public void LinkPublicFunction()
    {
        // ShortCuts.Link();
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
